import React from 'react'

const SectionTitleLeft = ({title}) => {
 return (
  <div className="section-title-left">
   <h2>{title}</h2>
   <div className="underline"></div>
  </div>
 )
}

export default SectionTitleLeft
